# 组织架构树

> 状态：已完成（2024-09-30）

## 背景

项目已经有了用户表、部门表、职位表以及它们的关系表，也完成了自动生成 CURD 的工具。这些基础设施就位后，可以做一个"组织架构树"功能来串联它们。

## 需求

一个用户可以加入多个部门，在不同部门里分别担任不同的职位。

## 实现方式

没有客户端，先编写 go test 通过 HTTP 访问的测试代码作为"客户端"来验证功能。

## 产出

- 用户-部门-职位多对多关系的数据模型和 CRUD 接口
- 对应 backlog 中序号 1
